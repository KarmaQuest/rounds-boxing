// Stub de remplacement pour Vitest : le package `server-only` lève une
// erreur quand il est importé hors de la condition "react-server" de Next.js
// (voir vitest.config.ts). Les tests unitaires de la couche données vivent
// côté serveur, ce marqueur n'a pas de sens pour eux.
export {};
